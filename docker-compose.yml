services:
  postgres:
    image: postgres:16-alpine
    container_name: orbitmind-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-orbitmind}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-orbitmind_dev}
      POSTGRES_DB: ${POSTGRES_DB:-orbitmind}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-orbitmind}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
